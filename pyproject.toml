[project]
name = "practmcp"
version = "0.1.0"
description = "Servidor MCP de práctica: expone una base de datos MySQL remota como herramientas para Claude"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "mcp[cli]>=1.8.0",
    "mysql-connector-python>=9.0.0",
    "python-dotenv>=1.1.0",
]
